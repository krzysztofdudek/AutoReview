---
description: List rules matching a file path
argument-hint: "<path>"
---

Run: `node ${CLAUDE_PLUGIN_ROOT}/scripts/bin/context.mjs $ARGUMENTS`

Follow each `read:` pointer with the Read tool before writing to the file.
