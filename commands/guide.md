---
description: Find rules relevant to a free-text intent
argument-hint: "<query text>"
---

Run: `node ${CLAUDE_PLUGIN_ROOT}/scripts/bin/guide.mjs $ARGUMENTS`

Reads the top-scored rules and answers the user's question citing their ids.
